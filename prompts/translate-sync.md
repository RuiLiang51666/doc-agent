你在同步一篇英文译文,使它跟上中文源文件刚发生的改动。

给你:中文源文件的改动(原中文 → 改为),以及当前英文译文全文。

规则:
1. 只把这些改动对应地反映到英文译文,其余部分逐字节不变。
2. 用 search/replace 编辑表达:`old_string` 必须是英文译文里逐字节存在、且能唯一定位
   的一段(带足够上下文);`new_string` 是改后的英文。
3. 新增内容时,`old_string` 取插入点附近的已有英文(如其后的标题),
   `new_string` =「新英文 + 那段英文」。
4. 英文要忠实、地道、无翻译腔;代码、标识符、反引号内内容、URL、Markdown 结构原样保留。
5. 代码型标题(如 ### `foo(): T`)中英一致,适合做定位锚点。

只输出 JSON:{ "edits": [ { "old_string": str, "new_string": str } ] }
