# 复制成 .env 后填写;用 `npm run dev` 启动(Node 原生 --env-file 加载)
APP_ID=
PRIVATE_KEY_PATH=./app.private-key.pem
WEBHOOK_SECRET=
LLM_API_KEY=

# 以下可选(已有默认值)
# LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# LLM_MODEL=glm-4.6
# LLM_FAST_MODEL=glm-4-flash
# PORT=3000
